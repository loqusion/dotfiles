#!/usr/bin/env bash

progname="$(basename "$0")"
cmd="${1:-toggle}"

case "$cmd" in
    toggle)
        yabai -m window --toggle float
        yabai -m window --grid 4:5:1:0:3:4
    ;;
    on)
    ;;
    off)
    ;;
    *)
        echo "${progname}: Invalid subcommand '$1'" >&2
        exit 1
    ;;
esac
